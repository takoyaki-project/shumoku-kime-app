# 種目決めアプリ　実装指示書（Claude Code向け・MVPフェーズ）

## これは何のプロジェクトか

町内会運動会（甲子園一番町／甲一）の参加種目割り当てを自動化するWebアプリ。
これまで役員が手作業で行っていた「集計・種目決定」を代わりに行う。
毎年担当者が交代するため、**プログラムを読めない人でも2ステップで使える**ことが最優先。

同じ場所にある以下のファイルを、すべて事前に読み込んでから着手してください。

- `うんどうかい係_設計ブリーフ.md` … 全体設計（データ構造・割り当てロジック・出力形式など）
- `種目マスタ.json` … 23種目の構造化データ（正規化ルール・エイリアス・小枠内訳を含む）
- `dummy_form_responses.csv` … テスト用の回答データ（意図的に難しいケースを含む）

---

## 今回（MVP）で作るもの

以下の一本道だけを動かす。装飾的なUIや種目マスタ編集画面は**今回は作らない**。

1. `dummy_form_responses.csv` の内容をアプリに読み込む（貼り付け or ファイル選択）
2. 重複送信（同一人物の再送信）を検出し、タイムスタンプが新しい方だけを残す
3. 実行ボタンで、設計ブリーフの2段階ロジックに従って割り当てを計算する
4. 結果をExcel（.xlsx）としてダウンロードできるようにする

これで一通り動けば、MVPとしては完成。**名簿との突き合わせ（名寄せ）は行わない**（大人の名簿が存在しないため）。氏名はフォーム回答の「名前」列（漢字想定）をそのまま使う。

---

## 技術要件

- HTML/JS（1〜数ファイル）。サーバー不要、ブラウザ内で完結させる。
- CSV解析・Excel書き出しは、papaparseやSheetJS等、ブラウザで完結するライブラリを使ってよい。
- **PCのブラウザで動かす前提**（スマートフォンではない。会長宅または本人宅の、インターネットに接続されたPCで実行する）。
- 外部サーバーへのデータ送信は一切行わない。

---

## 特に注意してほしい点

### ① 種目名の表記ゆれ
Googleフォームは年齢で分岐する構造のため、同じ種目の質問が複数回（最大3回程度）登場し、CSVには同じ見出し名の列が複数できる。
`種目マスタ.json` の `normalize_rule` に従って列名を正規化し、`aliases` を使って `種目マスタ.json` の該当種目（`events[].id`）に紐付けること。

### ② 重複列の扱い
同じ種目に対応する列が複数あっても、1人の回答者はフォームの年齢分岐により、どれか1列にしか値を持たない（他は空欄）。**空でない列の値を採用**すればよく、複雑な統合処理は不要。

### ③ 名前が非常に似た別種目が存在する
`種目マスタ.json` の `e14`（徒競走）と `e16`（徒競走（就学前児童））は、名前は似ているが**別の種目**。取り違えないこと。

### ④ 小枠（学年・性別・年代）の扱い
`種目マスタ.json` の `subquota` を持つ種目は、種目全体で1つの定員として扱わず、**小枠ごとに独立した定員として管理**する。判定に必要な性別・年齢の情報は、回答データの「性別」列と「年齢」列（`age_category_to_subquota_band` で変換）を使う。

### ⑤ 割り当てロジック
設計ブリーフ4章の2段階（保証パス→希望の上乗せパス）に厳密に従うこと。「出場希望」はチェックの有無のみで、○△×の3値ではない。優先度は「チェックした種目の総数が少ない人ほど高い」というルールで決まる（出られる種目数の申告や重み付けの質問はフォームに存在しない）。定員割れした種目に自動で人を割り当てる「おまかせ埋め」は行わない。定員割れは、Excel出力に「欠員」行として明記する（⑩参照）。

### ⑥ 矛盾ケース
自動で無理に解決しようとせず、Excel出力に「要確認」列を設けてフラグを立てる。

### ⑦ 備考欄
アルゴリズムでは一切処理しない。Excel出力にそのまま残すだけでよい。

### ⑧ 重複送信の扱い
「名前」「年齢」「性別」が完全一致する行が複数あれば、Googleフォームのタイムスタンプ列（実際のCSVには自動付与される）が最も新しいものを採用し、他は破棄する。タイムスタンプが読み取れない場合は、全件を出力し「重複疑い」フラグを立てて人間確認に回す。

### ⑨ 氏名の表記
名簿が存在しないため、氏名はフォーム回答の「名前」列（漢字想定）をそのまま出力に使う。「ふりがな」は重複判定の補助に使う。

### ⑩ 欠員行
定員割れした種目は、不足人数分だけ「氏名」列に「欠員」と入れた行を出力に追加する（他の列は空欄）。招集係が一覧を見ればすぐ気づけるようにするため。

---

## 完成の確認方法

`dummy_form_responses.csv` を実際に読み込ませて動かし、以下がちゃんと起きることを確認する。

- パンくい競争（定員5名）に対して、えー・びー・ひー・サユリさんの4人がどう割り振られるか（チェック数が少ない人＝ひーくん・サユリさんが確実に拾われるか）
- サユリさん（1種目のみチェック、競合種目）が、外れた場合に「要確認」として出力されるか
- ケンタくん（性別ミスマッチの希望）がどう扱われるか
- ハナコさん（応援のみ）がエラーにならず、割り当て対象から自然に除外されるか
- 田村夫妻の備考欄が、それぞれの出力行にちゃんと残っているか
- えーくん（一番　英太）の重複送信（2件目、タイムスタンプが新しい方）が正しく1件だけ採用されるか
- 定員に対して希望者が少ない種目（地区対抗リレー等）に「欠員」行が出力されるか

---

## UIについてのフィードバック（実装後の確認）

- 配色（グレー基調＋赤のアクセント）はこのままでよい
- **文字サイズが全体的に小さいので、もう少し大きく見やすくしてほしい**。読む人は40〜60代以上を含む役員なので、PC画面でも読みやすいサイズを優先すること

---

## 今回やらないこと（次フェーズ以降）

- 種目マスタの編集画面
- Googleフォームとの列名不一致の警告表示
- 見た目の作り込み（動けば十分。装飾は後回し）
