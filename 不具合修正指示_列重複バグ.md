# 種目決めアプリ　不具合修正指示（実データテストで判明）

## 症状

実際のGoogleフォーム回答CSV（70件）を読み込ませたところ、多数の種目で「出場希望者がいるのに欠員として出力される」不具合が発生した。特にデカパンサッカー・ペアで協力・女子紅白対抗つな引き・早飲み競走・徒競走で顕著。また「高学年の回答は読み込まれるが、低学年の回答が読み込まれない」という症状も確認された。

## 原因（確定）

実際のCSVは、年齢による分岐のため、**同じ種目名の列がヘッダー内に最大4回登場する**（例：「☆１９．みんなでヨーイドン」は4箇所、「☆６．ペアで協力」は3箇所）。

現在の実装は、この重複した列名を**キーにして値を格納する際、後から出てくる列の値で上書きしてしまっている**（ヘッダー名をキーとしたオブジェクト／辞書に、列の並び順のまま代入している状態と推測される）。

その結果、ある回答者が実際にチェックした種目の値が、後方にある同名の別セクション（本人には無関係で必ず空欄になる列）の値によって上書きされ、消えてしまっている。

検証の結果、実データ70件中、**151件分の出場希望チェックがこの上書きによって失われている**ことを確認済み。

## 修正方針

`種目マスタ.json` の `duplicate_column_rule` に立ち返ること。

> 同じ種目に対応する列が複数あっても、1人の回答者はフォームの年齢分岐により、どれか1列にしか値を持たない。空でない列の値を採用する。

具体的な修正：

1. CSVのヘッダーを読み込む際、**ヘッダー名をキーにしたオブジェクト化を行わない**。ヘッダーは配列のまま、列インデックスで扱う。
2. 種目マスタの各イベント（`aliases`含む正規化後の名前）について、該当するヘッダー名を持つ**すべての列インデックスを事前に集めておく**。
3. 1人の回答者の行を処理する際、その種目に該当する複数の列インデックスを順に見て、**最初に見つかった空でない値を採用する**。すべて空欄なら「チェックなし」として扱う。
4. これを23種目すべてに対して行う。

**やってはいけない実装**：重複したヘッダー名を検出して「列名」「列名_1」「列名_2」のようにリネームして区別する方法。これは重複列そのものの上書き問題は解決するが、種目マスタとの照合時にリネーム後の名前が一致しなくなり、`_1`以降の列が「未分類」として扱われ、そこに入っている回答が失われる。**リネームは行わず、常に列インデックスの配列として保持し、正規化後の名前が一致する列をすべて集めてから空でない値を探す**こと。

## 種目マスタ側の修正（別途対応済み）

`e16`（徒競走（就学前児童））について、実際のフォームの見出しは開きカッコの直後に全角スペースが入っている（`徒競走（　就学前児童）`）。`normalize_rule` を「内部の空白（全角・半角とも）もすべて除去して比較する」に更新済み。実装側もこの更新後のルールに従うこと。

## 種目マスタ側のもう一つの修正：学年の内訳が実データで判定不可能だった

`e04`・`e13`・`e15`・`e17` は、個別学年（1年・2年・3年…）ごとの内訳を持たせていたが、**実際のフォームの年齢選択肢は「小学１～３年生」「小学４～６年生」という学年帯までしか分からず、個別学年は判定できない**ことが判明した。この4種目は、判定可能な単位（学年帯全体、または種目全体）の定員にまとめ直し済み。実装側も、この4種目については個別学年での小枠判定を行わず、単一の定員として扱うこと。

`e17`（みんなでヨーイドン）は、内訳の合計（2+6+1=9）が`capacity_total`と一致するよう修正済み。以前は内訳の値をそのまま合計すると4にしかならず、実際の定員（9）と食い違っていた。今後、種目マスタの`subquota.breakdown`を変更する際は、**内訳の合計値が`capacity_total`と一致しているか必ず確認**すること。

## 確認方法

修正後、以下を確認すること。

- 今回発見した151件のロスが解消され、実データを再読み込みした際に、各種目のチェック済み人数が「元のCSVを目視で数えた人数」と一致する
- 特に、重複回数が多い種目（みんなでヨーイドン4箇所、ボルガの舟歌4箇所）で正しく集計されるか重点的に確認する
- ダミーデータ（`dummy_form_responses.csv`）でも引き続き正しく動くことを確認する（このファイルも同様の重複列構造を持つため、リグレッションが起きていないか確認できる）
